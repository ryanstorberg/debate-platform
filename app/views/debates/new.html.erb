<%= form_for :debate, url: debates_path do |f| %>
  <%= f.label :title, "Title" %>
  <%= f.text_field :title %>
  <%= f.label :body, "Body" %>
  <%= f.text_area :body %>
  <%= f.submit "Submit", :disable_with => 'Submiting...'  %>
<% end %>
