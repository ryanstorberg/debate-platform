<%= form_for [@debate, Comment.new] do |f| %>
  <%= f.hidden_field :agree, value: agree %>
  <%= f.hidden_field :debate_id, value: @debate.id %>
  <%= f.text_area :content, placeholder: placeholder %><br>
  <%= f.submit button_text, { class: button_class } %>
<% end %>
